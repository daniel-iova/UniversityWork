/**
 * This function is used to read data from a given file
 * and store it inside the Graph g object
 * @param g - struct Graph*
 * @param file - const char*
 */
void read_data(struct Graph* g, const char* file);
